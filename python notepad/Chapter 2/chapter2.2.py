2.2)
 (What’s wrong with this code?) The following code should read an integer into the
 variable rating: 
rating = input('Enter an integer rating between 1 and 10')

Ans: the code is supposed to use an if statement to make the program run within the limit of condition and also compel the user to input an integer within 1 - 10 range.
